import { Container } from "../../atom/Container"


export const HeroSection = () => {
    return (
        //! The container component can be removed if its not fitting into this component. Use a div instead and style it yourself. Look at the figma file for design.
        <Container>
            This is the hero section
        </Container>
    )
}